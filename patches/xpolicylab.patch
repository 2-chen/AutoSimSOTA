diff --git a/policy/ACT/setup_eval_policy_server.sh b/policy/ACT/setup_eval_policy_server.sh
index ea0bf98..59a4ac8 100644
--- a/policy/ACT/setup_eval_policy_server.sh
+++ b/policy/ACT/setup_eval_policy_server.sh
@@ -19,11 +19,16 @@ BENCH_ROOT="$(cd "${XPL_ROOT}/.." && pwd)"
 policy_name="$(basename "${SCRIPT_DIR}")"
 yaml_file="${XPL_ROOT}/policy/${policy_name}/deploy.yml"
 
-source "$(conda info --base)/etc/profile.d/conda.sh"
-conda activate "${policy_conda_env}"
+if [[ -n "${XPOLICYLAB_PYTHON:-}" ]]; then
+    PYTHON_BIN="${XPOLICYLAB_PYTHON}"
+else
+    source "$(conda info --base)/etc/profile.d/conda.sh"
+    conda activate "${policy_conda_env}"
+    PYTHON_BIN=python
+fi
 
 action_dim=$(
-    PYTHONPATH="${XPL_ROOT}" python -c "
+    PYTHONPATH="${XPL_ROOT}" "${PYTHON_BIN}" -c "
 import sys
 from XPolicyLab.utils.process_data import get_action_dim
 print(get_action_dim(sys.argv[1]))
@@ -48,7 +53,7 @@ echo "[SERVER] ckpt_dir=${ckpt_dir}"
 exec env \
     PYTHONWARNINGS=ignore::UserWarning \
     CUDA_VISIBLE_DEVICES="${policy_gpu_id}" \
-    python "${XPL_ROOT}/setup_policy_server.py" \
+    "${PYTHON_BIN}" "${XPL_ROOT}/setup_policy_server.py" \
         --config_path "${yaml_file}" \
         --overrides \
             port="${policy_server_port}" \
diff --git a/utils/run_sim_env_client.sh b/utils/run_sim_env_client.sh
index 4e305bf..2d0c068 100644
--- a/utils/run_sim_env_client.sh
+++ b/utils/run_sim_env_client.sh
@@ -15,9 +15,13 @@ env_gpu_id="${11}"
 policy_server_ip="${12:-localhost}"
 protocol="${13:-ws}"
 
-source "$(conda info --base)/etc/profile.d/conda.sh"
-conda deactivate || true
-conda activate "${eval_env_conda_env}"
+if [[ -n "${XPOLICYLAB_PYTHON:-}" ]]; then
+    export PATH="$(dirname "${XPOLICYLAB_PYTHON}"):${PATH}"
+else
+    source "$(conda info --base)/etc/profile.d/conda.sh"
+    conda deactivate || true
+    conda activate "${eval_env_conda_env}"
+fi
 
 echo -e "\033[34m[CLIENT] Activating Conda environment: ${eval_env_conda_env}\033[0m"
 echo -e "\033[34m[CLIENT] Connecting to server ${policy_server_ip}:${policy_server_port}...\033[0m"
@@ -35,4 +39,4 @@ bash "${root_dir}/scripts/eval_policy.sh" \
     --root_dir "${root_dir}" \
     --device_id "${env_gpu_id}" \
     --additional_info "${additional_info}" \
-    --seed "${seed}"
\ No newline at end of file
+    --seed "${seed}"
